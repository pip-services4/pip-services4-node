{
    "name": "pip-services4-mysql-node",
    "version": "1.0.0",
    "author": "Enterprise Innovation Consulting",
    "description": "MySQL components for Pip.Services in Node.js / ES2017",
    "contributors": [
        {
            "name": "Sergey Seroukhov",
            "email": "seroukhov@gmail.com"
        }
    ],
    "main": "./obj/src/index.js",
    "typings": "./obj/src/index.d.ts",
    "keywords": [
        "pip.services",
        "microservice",
        "mysql",
        "library"
    ],
    "noAnalyze": true,
    "repository": {
        "type": "git",
        "url": "ssh://git@github.com:pip-services4-node/pip-services4-mysql-node.git"
    },
    "license": "MIT",
    "engines": {
        "node": ">=14.0.0"
    },
    "private": false,
    "scripts": {
        "build": "tsc",
        "watch": "tsc --watch",
        "delay_test": "sleep 30 && echo Continue... && mocha -t 30000 -R spec -u tdd --recursive ./obj/test",
        "test": "mocha -t 10000 -R spec -u tdd --recursive ./obj/test",
        "retest": "tsc && mocha -t 10000 -R spec -u tdd --recursive ./obj/test"
    },
    "dependencies": {
        "mysql": "^2.18.1",
        "pip-services4-commons-node": "^1.0.0",
        "pip-services4-components-node": "^1.0.0",
        "pip-services4-data-node": "^1.0.0"
    },
    "devDependencies": {
        "@types/chai": "*",
        "@types/mocha": "^7.0.0",
        "@types/node": "*",
        "chai": "*",
        "mocha": "^7.0.0"
    }
}
